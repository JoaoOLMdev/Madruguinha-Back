# .env.example — expanded, safe example for Madruguinha-Back
#
# This file is an example only. It is based on your existing `.env` but
# with secrets redacted. Keep real secrets out of version control.
#
# Required
# Django secret key (REQUIRED) — generate a secure random value for production.
# e.g. `python -c "from django.core.management.utils import get_random_secret_key; print(__import__('os').urandom(24))"`
SECRET_KEY=replace_this_with_a_secure_random_secret_key

# Optional but recommended
# DEBUG: True/False (defaults to False in settings.py)
DEBUG=False

# DATABASE_URL: If unset, settings.py falls back to a local sqlite file.
# Format examples:
#   postgresql://USER:PASS@HOST:PORT/NAME
#   postgresql://dbuser:dbpass@db.example.com:5432/dbname?sslmode=require
# Example (redacted version derived from your .env):
DATABASE_URL=postgresql://<DB_USER>:<DB_PASS>@<DB_HOST>:<DB_PORT>/<DB_NAME>?sslmode=require

# Hosts and origins (comma-separated)
# ALLOWED_HOSTS defaults to 127.0.0.1,localhost when unset
ALLOWED_HOSTS=127.0.0.1,localhost

# CSRF trusted origins used when running behind proxies / different hostnames
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# SIMPLE JWT signing key. If unset, settings.py uses SECRET_KEY as fallback.
# Set this if you want a separate key for JWT tokens.
JWT_SIGNING_KEY=

# Email settings (optional; helpful for password reset and notifications)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=webmaster@localhost

# Monitoring / error reporting (optional)
#SENTRY_DSN=

# Static/media (optional)
#STATIC_ROOT=/var/www/madruguinha/static

# Notes:
# - Replace placeholders (<DB_USER>, <DB_PASS>, etc.) with your real values
#   when creating a local/.env for development or a secure env for production.
# - Never commit your real `.env` to source control. Commit only this file
#   or a production-specific `.env.example` with placeholders.
